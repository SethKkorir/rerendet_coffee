<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <style>
        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            line-height: 1.6;
            color: #333;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
        }

        .header {
            background: #1a1a1a;
            padding: 40px;
            text-align: center;
            border-radius: 12px 12px 0 0;
        }

        .header h1 {
            color: #d4af37;
            margin: 0;
            font-size: 28px;
            letter-spacing: 1px;
        }

        .content {
            padding: 40px;
            border: 1px solid #eee;
            border-top: none;
            border-radius: 0 0 12px 12px;
            background: #ffffff;
        }

        .greeting {
            font-size: 18px;
            font-weight: bold;
            margin-bottom: 20px;
        }

        .message {
            color: #555;
            margin-bottom: 25px;
        }

        .code-box {
            background: white;
            padding: 30px;
            text-align: center;
            margin: 30px 0;
            border: 2px dashed #10b981;
            border-radius: 12px;
        }

        .code {
            font-size: 2.5rem;
            font-weight: bold;
            color: #10b981;
            letter-spacing: 5px;
        }

        .warning-box {
            background: #fff3cd;
            border: 1px solid #ffeaa7;
            padding: 15px;
            margin: 20px 0;
            border-radius: 8px;
            font-size: 14px;
        }

        .footer {
            padding: 30px;
            text-align: center;
            font-size: 13px;
            color: #999;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <h1>Rerendet Coffee</h1>
        </div>
        <div class="content">
            <div class="greeting">Hello {{firstName}},</div>
            <p class="message">Thank you for joining Rerendet Coffee. Please use the verification code below to activate
                your account:</p>

            <div class="code-box">
                <div class="code">{{verificationCode}}</div>
            </div>

            <div class="warning-box">
                <strong>⚠️ This code will expire in 10 minutes.</strong>
            </div>

            <p class="message">Enter this code on the verification page to start exploring our premium coffee selection.
            </p>
        </div>
        <div class="footer">
            &copy; 2025 Rerendet Coffee. All rights reserved.
        </div>
    </div>
</body>

</html>